// dashboard.js - Summary View (Optional)

function renderDashboard() {
  // Currently redirecting to Roadmap in app.js
  // But if we ever show this:
  return `
    <div class="animate-fade-in">
        <div class="light-card">
            <h2>Dashboard Redirect</h2>
            <p>Loading your empire...</p>
        </div>
    </div>
  `;
}
